import React from 'react'

function MovieControls({movie}) {
    return (
        <div className="inner-card-controls">
            <button className="ctrl-btn">
                <i className="fa-fw far fa-power-off"></i>
            </button>
        </div>
    )
}

export default MovieControls
